{
  "name": "@conduit/live-server",
  "version": "0.1.0",
  "private": true,
  "description": "Local Puppeteer-based backend for live bank testing",
  "scripts": {
    "start": "tsx server.ts",
    "dev": "tsx watch server.ts",
    "postinstall": "npx puppeteer browsers install chrome",
    "test:chase": "tsx test-chase-e2e.ts",
    "test": "npx jest --config jest.config.js --forceExit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "puppeteer": "^24.0.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.5.0"
  }
}
