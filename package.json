{
  "name": "@conduit/sdk",
  "version": "0.1.0",
  "description": "Plaid competitor — Expo SDK with embedded browser automation for banking data extraction",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --forceExit",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": ["banking", "plaid", "fintech", "sdk", "authentication", "mfa"],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.5.0"
  }
}
