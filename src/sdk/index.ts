/**
 * SDK module — high-level types and utilities for host app integration.
 */

export { PreviewStatus, type PreviewStatusName, type PreviewState } from './types';

export { ConduitLink } from './ConduitLink';
export type {
  ConduitLinkConfig,
  ConduitLinkEvent,
  ConduitLinkEventListener,
} from './ConduitLink';
